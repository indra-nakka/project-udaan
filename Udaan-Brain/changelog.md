# Changelog

- 2026-07-06 | build_models.py | **Playset + swing rebuilt.** **Playset**: the gable roof panels were sloped the WRONG way (peaked at the eaves → looked floating/tilted); corrected the rotation signs so inner edges meet high at the ridge and eaves rest on the posts; pyramid roof lowered onto the posts; **spiral slide** replaced (jumbled stacked tiles → one clean fat helical TUBE via `seg_between`). **Swing (jhoola)**: legs no longer overshoot the top bar — rebuilt as a proper A-frame (two legs meet at an apex at each end via `seg_between`, top bar spans the apexes), hangers drop straight to the bench corners. Blender-only; needs build-models.bat + reimport | session-074
- 2026-07-06 | Music.cs, build_models.py, ParkMapGenerator.cs, Music-Tuning-Guide.md(new) | **Fast/upbeat music + tuning guide + dome/tyreswing fixes + tighter spacing.** **Music**: user wanted it fast — battle now 136bpm bright/busy, boss 138bpm driving-minor (both kept warm via the sub-octave + 8-bar loops so they're lively, not grating). Wrote **Music-Tuning-Guide.md** — a plain-language how-to (every knob, recipes, a worked melody example, copy-paste presets). **Model fixes from preview review** (checked the previously-unseen renders): **park_dome** was rendering as a full sphere (buried-half trick fails under ground-align) → rebuilt as a TRUE hemisphere (semicircle meridian ribs via new `seg_between` helper + profile rings, all z≥0); **park_tyreswing** chains were crossing in the middle → now fan cleanly from the beam to 3 tyre-rim points. (Verified tyrewall/trampoline/bench/sandbox previews — all good, no change.) **Overlap**: still a bit → propSpacing 3→5, footprint radius inflated ×1.12, retries 40→60. Subagent-reviewed clean. Needs build-models.bat (dome + tyreswing) + reimport | session-073 **Music** (user: battle/boss "annoying"): battle → gentle warm SINE theme, boss → slow low ambient bed; both now 8-bar loops (less repetitive), sparser melodies, held bass, softer attack, warm sub-octave instead of the bright shimmer, and BgmVolume 0.34→0.24. Menu unchanged. **No-overlap placement**: `ParkMapGenerator` now measures each spawned model's footprint radius and rejects positions colliding with already-placed props (`_placed` list + `FindOpenSpot`, 40 retries, `propSpacing` gap); Awake reordered big→small so hero pieces claim ground first. **Density reverted** to the pre-40% counts (walls stay gone). **Model tweaks:** playset gable roof tightened (ridge gap closed); animal merry-go-round animals rebuilt with legs + elongated body + snout + ears + tail (were stacked boxes); rock-wall back support → clear angled A-frame + feet; jhoola seat-backs lowered + thinner hangers. Subagent-reviewed clean. Needs build-models.bat (tweaked FBX) + reimport | session-072 Added the last 5 reference-photo stations: **park_rockwall** (holds on an A-frame panel), **park_tyrewall** (5×4 colour-banded tyre grid), **park_trampoline** (blue rim + jump mat), **park_bench** (green slats), **park_animalmerry** (spinner with animal seats) — all modelled, wired through ParkProps/DemoFlow/DemoSceneBuilder, scattered by ParkMapGenerator via a generic `BuildProp` helper. **Density +40%** on every existing count (trees 36, towers 20, etc.). **Slide + playset get an extra 2×** on top of propScale (via SpawnProp scale arg). **Model tweaks from preview review:** playset barrel roof → tidy **gable roof** + connected/thickened straight slide + neater spiral; merry-go-round grab bars → clean **inverted-U handholds** (were tumbling); jhoola swing leg splay reduced. Subagent-reviewed clean end-to-end. Needs build-models.bat (5 new FBX) + reimport + "Create/Update Demo Scene" | session-071 User approved the new park models → **global `propScale = 2`** in ParkMapGenerator (multiplies every model at spawn; ground-align keeps bases on the floor; no rebuild needed). **Removed the maze walls entirely** — the larger scattered park items now form the natural maze; bumped counts (playsets 3, merries 4, tyre-swings 4, towers/giant-trees 14) and retuned the giant-tree scale so it stays sane at 2×. Cleaned out the now-orphaned **hedge**: deleted `build_park_hedge` + export, `ParkProps.Hedge`, `DemoFlow.hedgeModel`, the DemoSceneBuilder load, `BuildWallCluster` + `SpawnStretched`. (Old `park_hedge.fbx` in Assets/Art/Models is now unused — safe to delete.) | session-070
- 2026-07-06 | build_models.py, ParkProps.cs, ParkMapGenerator.cs, DemoFlow.cs, Editor/DemoSceneBuilder.cs | **Real Indian-park stations (from user reference photos).** User shared photos of an actual Indian playground → built proper equipment instead of plain primitives. New models: **park_playset** (the HERO combined structure — two roofed towers [arched + pyramid canopy], bridge, straight slide, spiral slide, monkey bars), **park_merry** (merry-go-round: segmented colour disk + yellow U grab-bars), **park_dome** (multi-colour geodesic dome climber — replaces the box gym), **park_tyreswing** (tyre torus on chains), and reworked **park_swing** → colourful **jhoola** (A-frame with two facing bench seats). Added `torus`/`cone` Blender helpers. Wired 4 new models through `ParkProps`/`DemoFlow`/`DemoSceneBuilder`; `ParkMapGenerator` routes gym→dome and scatters playset (rare)/merry/tyre-swing. Fixed a **pre-existing duplicate `torus()`** (old 4-arg def shadowed the new one → would've crashed the dome build). Subagent-reviewed. First pass — needs build-models.bat + preview review to refine to the refs. Still TODO from the photos: rock wall, tyre-climbing grid, trampoline, park bench, animal merry-go-round | session-069
- 2026-07-06 | Music.cs(rewrite), build_models.py, ParkMapGenerator.cs, ParkProps.cs, DemoFlow.cs, Editor/DemoSceneBuilder.cs, Design-Audio-and-QA.md | **Music rework + park-item fixes (feedback pass).** **Music**: refactored `Music.cs` into an editable `Song` table (MENU/BATTLE/BOSS) with a full field guide + a "how to tweak" table in Design-Audio-and-QA. Battle reworked from arcade-pump → **mid-tempo melodic flight theme** (airy rests, soft pad, vibrato); Boss reworked → **slow, sparse, atmospheric minor** with a sustained low drone (tension over speed). New synth layers: pad chords, per-note vibrato, octave shimmer. **Park models fixed**: the **slide was backwards** (chute tilted up & detached) → now **descends from a raised deck** with a back ladder + side kerbs; **swing got chains** (seats no longer float); **preview camera now frames each model by bounding box** (tall props were cropped). **Walls/towers → real park cover**: `ParkMapGenerator` now renders maze walls as **tall leafy hedges** (`park_hedge` stretched per segment) and climbing towers as **giant shade-trees** (scaled `park_tree`), replacing the greybox — primitive fallback kept. New `park_hedge` model + `ParkProps.Hedge` wired through DemoFlow/DemoSceneBuilder. All subagent-reviewed clean. Needs build-models.bat re-run (park_hedge + fixed slide/swing) + Editor compile | session-068
- 2026-07-06 | Music.cs(new), MissionDirector.cs, DemoFlow.cs, TargetingSystem.cs, ParkProps.cs(new), ParkMapGenerator.cs, build_models.py, Editor/DemoSceneBuilder.cs, Design-Audio-and-QA.md, Demo-Scope.md | **Audio set (#93) + targeting/park/menu batch.** Model pipeline confirmed working in Unity (models import upright at uniform scale). **Procedural BGM** `Music.cs`: three code-synthesized looping moods — **Menu** (calm major), **Battle** (upbeat), **Boss** (tense minor + low drone + tritone stabs) — lazily built, cached, crossfaded on a persistent 2D source (sits under SFX); cued from DemoFlow (menu/play/results) + `MissionDirector.SpawnBoss` (→Boss). **Predictive targeting lead**: `TargetingSystem.AimDir` now leads a moving target using (target−own) velocity × projectile time-to-hit (2-iter solve) → bullets actually connect on movers (was locking to present position = guaranteed miss). `leadTargets`/`projectileSpeed` exposed; `AimPoint` published for future HUD reticle. ⚠️ lead applies to enemy aim too (sharper on Hard/Pain — watch-item). **Guns on enemy drones**: `_blaster("enemy")` added to enemy_binbot + enemy_brasspod (only player tiers had visible blasters). **Real park items**: 6 park-prop models (tree/slide/swing/gym/sandbox/seesaw) built in Blender; `ParkProps` holder + `ParkMapGenerator` instantiates them (base-aligned to ground + MeshCollider) with primitive fallback; auto-wired by DemoSceneBuilder. **Menu cleanup**: dropped "build. fly. play." subtitle + volume ± control; added **Level: Children's Park** pill. Needs build-models.bat re-run (6 new FBX) + Editor compile | session-067
- 2026-07-06 | build_models.py, Editor/UdaanModelPostprocessor.cs(new), FactionVisuals.cs, CombatSpawner.cs, DemoFlow.cs, Design-Model-Pipeline.md(new) | **Blender→Unity model pipeline standardized.** Root cause of the `(11.6,217,4.2)` scale = axis + unit mismatch (rotated + wrong scale → per-axis stretch). Fix: export now **bakes orientation** (`bake_space_transform` + `transform_apply` + unit scale); a folder-wide **AssetPostprocessor** sets `useFileScale`/`globalScale=1` on every FBX in `Art/Models` (bakeAxisConversion off to avoid double-bake — one-flag flip if still rotated); **per-faction rotation nudges** (`FactionVisuals` eulers + DemoFlow) for nose-yaw. New `Design-Model-Pipeline.md` = the standard. Untested in Unity this session (flagged the fallback flag) | session-066

- 2026-07-06 | FactionVisuals.cs(new), CombatSpawner.cs, DemoFlow.cs, Editor/DemoSceneBuilder.cs | **Per-faction models (#4).** CombatSpawner now swaps in a **distinct body per faction** instead of the reused tinted player prefab: **enemy → enemy_binbot** (red tint), **ally → player quad** (blue tint), **boss → the purple octacopter** (native colors, no tint). `AttachSkin` disables the greybox renderers and parents the FBX model as a "Skin" child (colliders stripped). Models + tunable scales held in `FactionVisuals`, set from DemoFlow (auto-assigned by "Create or Update Demo Scene"). Fixes samey/invisible factions + finally puts the octacopter on screen. Fallback = greybox+tint if models unset. Subagent-reviewed clean (note: bossScale is relative to the 2.2× boss entity scale) | session-066
- 2026-07-06 | EnemyDroneAI.cs, Sfx.cs, DroneFlow/DemoFlow.cs, Editor/DroneSkinner.cs, build_models.py | **Ground-stick + audio + prop fixes.** **AI ground avoidance**: drones now hold a **minimum altitude** (downward ray → `s.altitude` vertical thrust) and the stuck-recovery **climbs straight up** — no more scraping/sticking to the floor. (Confirmed `altitude>0 = up` in DroneFlightController.) **2D SFX**: `Sfx.Play` now uses a `spatialBlend=0` AudioSource so sounds are audible in **third-person** too (PlayClipAtPoint's 3D falloff made TPV quiet). **Translucent props**: PROPBLUR alpha→0.5 + editor helper **"Udaan ▸ Make Prop Disks Translucent"** (flips PropBlur materials to URP transparent). **Result stars** fixed from pentagon-blob to a proper chunky cartoon star (inR 0.40→0.26) | session-066
- 2026-07-06 | BossController.cs(new), CombatSpawner.cs, Sfx.cs, DemoFlow.cs, TouchFlightHUD.cs, build_models.py | **Boss specials + polish.** **Boss brain** (#92): octacopter now makes an **entrance (evil-laugh SFX + shake)**, then alternates two **telegraphed** specials — a **SLIME-THROWER** fan (green globs that gunk you) and a **SHOCKWAVE** ring (readable pulse → expanding blast + knockback). Procedural `Sfx.BossLaugh` placeholder (authored in #93). **Results stars**: chunkier cartoon shape + **bouncy staggered pop-in** (StarPop, unscaled time). **Hide gameplay HUD** on completion. **Playful font across the whole HUD** (Comic Sans-style OS font). **Drone props → spin-blur disks** (no animation needed) in the model script. Hit-pops confirmed player-hits-only. C# subagent-reviewed clean | session-066
- 2026-07-06 | DemoFlow.cs, HitPops.cs(new), Editor/DroneSkinner.cs(new), SinglePlayerBootstrap.cs, Design-Maps.md(new) | **Results polish + hit juice + skinner + map system.** Results screen: **3 real star icons** (procedural star sprite), removed "bots bopped", **personalized outcome messages** with the pilot's name + variants (flawless/close-one/try-again → replayability), **playful OS font**. **HitPops** (#damage-numbers): playful floating pops on the player's hits — bouncy colored numbers + occasional "POW!/SPLAT!" on big hits (toggleable). **DroneSkinner** editor helper ("Udaan ▸ Skin Player Drone (T0)"): swaps greybox for the FBX skin + hides old renderers. **Design-Maps.md**: shift to authored layout + spawn posts + procedural detailing + city theming (map = the city connection; scalable ruleset). All subagent-reviewed clean. Map tasks #96–98 | session-066
- 2026-07-06 | DemoFlow.cs, MissionDirector.cs, Vfx.cs, TargetHealth.cs, DroneWeapon.cs | **Menu polish + victory/defeat page + cartoon reskin** (⚠️ compile+test). Start menu re-laid-out (stacked centered rows — fixes clipped labels; green PLAY). New **kid results screen** (#95): "YOU DID IT!" + N/3 Stars + time/kills, or "Oops — try again!" + reason, with Play Again / Main Menu — driven by new `MissionDirector.OnMissionEnd` event (R-replay kept only for the legacy scene). **Cartoon defeat** (#94): `Vfx.Poof` = grey smoke puffs + a tumbling `DeathSpiral` wreck (no fiery gore); Pop() uses it. **Toy projectiles**: bullets → cyan foam darts, rockets → green slime globs. Subagent-reviewed clean (★ glyphs → worded "N/3 Stars" since builtin font lacks them) | session-066

- 2026-07-06 | GameConfig.cs(new), DemoFlow.cs(new), Editor/DemoSceneBuilder.cs(new), SinglePlayerBootstrap.cs | **Demo scene + Start/Pause menus** (⚠️ compile + test). New **DemoFlow** builds a runtime Start menu (title, **name entry** default Ira, **difficulty Easy/Medium/Hard/Pain**, volume ±/mute, Play, Quit) and a **Pause overlay** (Resume/Restart/Main Menu; Esc or P), then launches gameplay via SinglePlayerBootstrap. **GameConfig** persists name/difficulty/volume (PlayerPrefs) and feeds difficulty into MissionDirector. **Editor menu "Udaan ▸ Create or Update Demo Scene"** generates `Assets/Scenes/Demo.unity` (DemoFlow + Drone_Player + EventSystem) and adds it to Build Settings. Subagent-reviewed clean (input handling = Both, so uGUI works). Tasks #89/#90/#81 done | session-066
- 2026-07-06 | MissionDirector.cs, build_models.py, Demo-Scope.md(new), Design-Weapons-and-Tools.md, Design-Story.md | **Demo-focus + tone/naming decisions.** Scope locked: finish the **demo** (re-skinned combat arena); story/cities/garage/mixed-missions = **Phase +1** (new `Demo-Scope.md` = Definition of Done + build order). Decisions: **difficulty → Easy/Medium/Hard/Pain** (code done; Pain = 1 life); **defeat = PG cartoon** (Wile E. Coyote smoke+spiral, only toy/property damage, no human/animal harm); **snowball → sticky slime**; **hero name player-selectable, default `Ira`** (game title Udaan stays separate). Added **purple octacopter boss** model (8 rotors, red eye, chin weapon) to the build script. Cleanup: removed dead `coreRegenDelay`, cleared scratch. Demo tasks #89–95 created | session-066
- 2026-07-06 | Design-Story.md(new), UDAAN_ROUTER.md | **Story & narrative design.** Wrote the story doc from research: underdog hero **Udaan**, a paraplegic wheelchair-using girl who dreams of flying; dream→wake→birthday-drone opening; hero's-journey arc; **8 Indian-city chapters** (Hyderabad home → Mumbai finale, Pokémon-style) each themed to real culture with fitting race/delivery/rescue/photo missions (Ahmedabad kite festival = flying set-piece; Mumbai dabbawala run = finale); FPV sim→whoop→build ladder tied to the garage/tier system. Strong **disability-representation guardrails** (agency, full personhood, disabled joy; **no cure/inspiration-porn/supercrip**; obstacles always external — hire a disabled consultant). Show-don't-tell / assume-smart-player throughout | session-066
- 2026-07-06 | UDAAN_ROUTER.md(rewrite), Game-Design-Document.md, 🎮 Combat & Game Vision.md, Design-Weapons-and-Tools.md | **Brain health check + reconciliation.** Code audit: **healthy** (no compile blockers; registry/difficulty/telemetry/retaliation consistent; only P2 dead code: coreRegenDelay, SpawnTargets). Docs audit found the entry Router still described the pre-pivot adult PvP MOBA → **rewrote UDAAN_ROUTER** as the correct entry map (pivot banner, current reading order, legacy section), declared **Game-Design-Document.md the single source of truth**, stamped **🎮 Combat & Game Vision superseded**. Folded **weapons mount-ladder research** (crude DIY → sealed turret, mapped to toy reskins) into Design-Weapons-and-Tools. Big uncommitted backlog **pushed to git** (main). Kicked off story research (cities/FPV/representation) | session-066
- 2026-07-06 | Design-Garage-and-Learning.md(new), Design-Weapons-and-Tools.md(new), Game-Design-Document.md, build_models.py, Art-Direction.md | **AUDIENCE PIVOT → kids 5–10, learn + play.** Udaan reframed: build a real-ish quadcopter in a **garage** (stealth STEM via visible component trade-offs: battery/props/frame/motors/wiring/remote) → play **mixed playful missions** (delivery, rescue, race, photo, skirmish). Weapons are **non-lethal toys** (foam/water/bubble/paint/snowball/sparkle); enemies gently **power down**, never destroyed. Wrote the garage/learning + playful-weapons design docs; rewrote GDD vision & pillars ("the garage is the teacher"). Model: **player quad ladder T0/T2/T3/T5 built & confirmed** (junk DIY → ducted-sensor), now with per-tier **toy blaster mounts**; Core + Outpost built. Decisions locked: playful non-lethal / mixed missions / stealth-learning | session-065
- 2026-07-06 | Art-Direction.md, build_models.py, MissionStats.cs, MissionDirector.cs, Design-*.md | **Green-flag batch (while away).** **Art pivot**: PLAYER drone = *realistic quadcopter* (grounded in real market: DIY plywood → Toy → Consumer/Neo → Prosumer/Mavic gimbal → FPV carbon → ducted-sensor Ascendant); the earlier junk/brass fantasy builds become ENEMIES. Believable prop/body ratio, cute, physics-plausible. **Blender pipeline expanded** to build the full player ladder (T0/T2/T3/T5) + Core + Outpost + 2 enemies in one headless run (syntax-checked). **#80 Telemetry**: every run appends a row to `persistentDataPath/udaan_runs.csv` (feeds the balance model). **#70 Difficulty presets**: Recruit/Pilot/Ace multipliers on enemy HP/dmg, boss, Core HP, and lives (spawn-time only, no restart-stacking) — subagent-verified clean. **Design specs** written: onboarding/tutorial, soft-lock utility-score targeting, audio + QA plan. Batch-1 code (#66/#71/#72) confirmed compiled (only the benign Input Manager deprecation warning). ⚠️ This batch (#70/#80) still needs one Editor compile to confirm | session-064
- 2026-07-06 | MissionDirector.cs, TargetingSystem.cs, TouchFlightHUD.cs, Balance-Model.xlsx | **Code batch 1** (⚠️ needs compile.log). **#66 Defend rebalance**: Core regen is now **continuous** (was gated to "no attacker nearby", which meant a single in-range attacker killed it in ~16s). Rate 45→**55** = just above one attacker's DPS, so a lone straggler is survivable (net +5/s) but 2+ overwhelm it; objective text now reads heavy/single/clear. Balance model updated (break-even = 1.1 attackers). **#71** TargetingSystem sort: precompute keys once + reuse one cached comparer (kills the O(n log n) GetComponent storm at 10Hz/drone). **#72** HUD ammo/stat text only reassigned on change (no per-frame canvas rebuild) | session-064

- 2026-07-06 | Udaan-Brain docs (Game-Design-Document.md, Enemy-Roster-and-Campaign.md, Market-Research-2026.md, architecture/Performance-and-Architecture-Audit.md, Balance-Model.xlsx) | **"While away" deliverables**: consolidated GDD; enemy roster (7 archetypes) + campaign ladder (6 missions) + difficulty presets; 2025-26 market/competitive brief (Udaan's touch-6DOF-PvE niche is unserved; auto-aim + monetization guidance); architecture & perf audit; live **balance model** (Wave scaling, TTK, Defend survival). ⚠️ Balance model flagged a real issue: at the wave-2 Defend tier a single sustained attacker (50 DPS) out-damages Core regen (45 HP/s) and kills the 800-HP Core in ~16s — Defend fairness relies entirely on the peel/kill loop keeping attackers at zero. Consider regen > single-attacker DPS | session-063

- 2026-07-06 | TargetHealth.cs, Outpost.cs, TargetingSystem.cs, EnemyDroneAI.cs, MissionDirector.cs, TouchFlightHUD.cs | **Performance refactor** (⚠️ needs Editor compile — I couldn't stand up an offline compiler, sandbox blocks the download). Added a self-maintained **static target registry** (`TargetHealth.All`, `Outpost.All`, register in OnEnable / remove in OnDisable) and replaced the per-frame/10Hz `FindObjectsByType<TargetHealth>` scans in RefreshOnScreen, radar, NearestCombatant, EnemiesNear, CountLiveEnemies, DrawOutposts — was ~180 full-scene allocating scans/sec across drones, now zero-alloc list iteration. Converted AI physics (LOS/clearance/stuck) and Outpost capture check to **NonAlloc** raycasts/overlaps with shared buffers (size 24). Objective HUD text now **de-dupes** unchanged strings. Subagent-reviewed for compile/lifecycle correctness | session-063

- 2026-07-06 | EnemyDroneAI.cs | **Obstacle avoidance rewrite** (enemies still got stuck). Root cause: a single forward `SphereCast` from the drone's own position can't detect a wall it's already overlapping, so once wedged it saw nothing and stayed. New system: (1) **stuck-recovery** — if commanded to move but barely moving next to solid geometry (velocity + `OverlapSphere` check), reverse out + spin + climb for 1.2s with a random turn dir; (2) **three forward whiskers** (fwd + both front-quarters) measure clearance and steer toward the more open side instead of the unreliable surface-normal; (3) **stop driving into walls** — cut throttle when close, reverse when very close. Raycast whiskers ignore other drones (only terrain blocks) | session-062

- 2026-07-06 | EnemyDroneAI.cs, TargetingSystem.cs | **Enemy self-defense / retaliation**: getting shot now makes an enemy break off its current target (e.g. tunneling the Core) and hunt the **nearest attacking combatant** for `retaliateDuration` (4.5s, refreshed by each hit). Both the nose AND the weapon's aim-assist follow the attacker — added `TargetingSystem.ForceTarget/ReleaseForcedTarget`. NearestCombatant skips the Core (targets only drones with a flight controller). This gives the player leverage in Defend: shooting the Core's attackers peels them off, letting the Core self-repair. Applies to allies too (they defend themselves) | session-061

- 2026-07-06 | MissionDirector.cs, TouchFlightHUD.cs | **Defend beat clarity + fairness rework** (Core kept dying / objective unclear). Added a world-tracked **CORE marker**: a cyan tag with live HP% that sits on the Core on-screen and becomes an **edge arrow** when it's behind you (`TouchFlightHUD.SetObjectiveMarker`). Added a tall **blue light-beam beacon** on the Core so it's visible map-wide. **Self-repair loop**: if no enemy is within `coreSafeRadius` (45m) for `coreRegenDelay` (2.5s), the Core heals at `coreRegenRate` (45 HP/s) — so clearing attackers now actively saves it; objective text calls out "UNDER ATTACK — kill the N near it" vs "safe — repairing". Enemies now **split aggro** (Core targetPriority 5→3) instead of all diving it; `defendDuration` 40→35s, `defendMaxEnemies` 6→5 | session-060

- 2026-07-06 | EnemyDroneAI.cs, CombatSpawner.cs, MissionDirector.cs, MissionStats.cs, TargetHealth.cs, Outpost.cs, TouchFlightHUD.cs | Big combat/UX pass. **AI vs walls**: enemies now check **line-of-sight** before firing (no more shooting through walls) and **steer around obstacles** ahead (SphereCast bank + climb) so they stop burying themselves in geometry. **Low-HP behaviour** changed from full flee to a **kite** — they back off but keep firing. **Progressive difficulty**: wave enemies scale HP/damage/fire-rate per wave (`perWave*`), **boss buffed** (HP 700→1200, faster/harder weapon, longer reach, presses attack). **Core defense fixed/fairer**: 2 allied **guardians** spawn to help hold it, and the push cap now counts *all* live hostiles (incl. outpost-spawned) so it can't runaway-swarm. **Outpost capture buff**: securing an outpost = full heal + **40% overshield for 30s** (reconciled single-bonus, plays nice with Armor upgrade). **Run summary** gains: enemy kills split **(you / allies)**, **allies lost**, and **ally damage** — via new `TargetHealth.lastAttacker*` kill attribution. **Tactical radar**: added owner-colored **outpost blips** (white/blue/red squares), a **terrain toggle** (key T, faint map footprints), and the center self marker is now an **upward triangle** instead of a dot | session-059

- 2026-07-06 | MissionStats.cs, MissionDirector.cs | Run summary now prints **Why** on a loss (defeat reason + phase): "Ran out of lives…" or "The Core was destroyed by the enemy push", tagged with the phase it happened in (e.g. "during Defend") | session-058

- 2026-07-06 | Outpost.cs(rewrite), MissionDirector.cs, CombatSpawner.cs, ParkMapGenerator.cs | **Contestable outposts**: outposts are now fought over by BOTH sides. Small beacon inside a translucent capture *bubble*; whichever team is alone inside fills the bar and flips ownership — orb goes white(neutral)→blue(player)/red(enemy), glow switches on when owned. An owned outpost periodically spawns that team's drones (allies for you, hostiles for the enemy), so enemies can steal outposts and reinforce. Placement is now **random** (spaced, min/max radius band) instead of a fixed ring. **Allies nerfed** (fire rate 0.5→0.85s, dmg 18→9) so the player stays the main threat. **Core buffed** 500→800 HP (it was the enemy push, not friendly fire, that blew it up — friendly fire is off). Map made **denser + taller**: more props, towers 20-32→30-52, walls 18-30→26-46, longer wall runs | session-057

- 2026-07-06 | MissionStats.cs(new), MissionDirector.cs, TargetHealth.cs, Projectile.cs, DroneWeapon.cs, CombatSpawner.cs, TouchFlightHUD.cs | **Run scorecard**: printed Console summary at Victory/Defeat — result, total time, per-wave clear times, kills, outposts captured, allies spawned, damage dealt/taken, lives used/remaining. Threaded a **`fromPlayer`** flag through the damage path (Projectile→TargetHealth `OnAnyDamaged`) so damage is attributed to the player vs allies; added `TargetHealth.OnDeath`; hitmarker now fires only on the player's own hits | session-056

- 2026-07-06 | TargetHealth.cs, MissionDirector.cs, Pickup.cs, Outpost.cs | Fixes: **Core insta-defeat** bug (Defend read the core at 0 HP before its Start ran → `TargetHealth.Configure` fills HP immediately); pickups **player-only** (allies were grabbing them); clearer per-phase **objective text** ("OBJECTIVE: …"); stronger **outpost-capture visual** (emissive fill during capture + bright claimed pop via Vfx) | session-055

- 2026-07-06 | MissionDirector.cs | Full "Sky Sentinel" arc: added **Defend** beat (spawn a team-1 Core with high target-priority so enemies swarm it + a timed push; survive `defendDuration` = advance, core destroyed = defeat) and **Upgrade** beat (pick fire-rate / damage / armor via 1-2-3 or gamepad A/B/X, applied to the player). New flow: intro → skirmish → capture → defend → upgrade → boss | session-054

- 2026-07-06 | Outpost.cs(new), CombatSpawner.cs, MissionDirector.cs, TouchFlightHUD.cs | **Allies + outposts arc**: `CombatSpawner.Ally` (team-1 AI, blue, targets enemies); `Outpost` capture beacon (fly into the blue zone to capture → spawns allied drones on a timer, up to a cap); MissionDirector gets a **Capture phase** (intro → wave 1 → capture outposts → waves 2–3 with allies → boss) + `ClearAll` on restart; radar now colours allies blue vs enemies red | session-053

- 2026-07-06 | Vfx.cs(new), Sfx.cs(new), CameraController.cs, Projectile.cs, DroneWeapon.cs, TargetHealth.cs, TouchFlightHUD.cs | **Juice pass v1**: code-built VFX (expanding-fade explosions on death + rocket splash, bullet-impact sparks, tracer trails on projectiles), **camera shake** (on explosions by distance + when the player takes damage), and **procedural SFX** (synthesized bullet/rocket/hit/explosion clips — no asset files; player-only fire sfx). Music/authored audio deferred (needs real assets) | session-052

- 2026-07-06 | MissionDirector.cs, TargetHealth.cs, TargetingSystem.cs | Mission depth: **3 lives** with safe respawn (heal + relocate high/centre + 2.5s spawn protection), Defeat only when out of lives, lives shown in HUD; **3s launch stealth** (untargetable + invulnerable) to orient; `TargetHealth.targetable` + `SetProtected` (targeting/damage respect it) | session-051

- 2026-07-04 | MissionDirector.cs(new), CombatSpawner.cs(new), TargetHealth.cs, SinglePlayerBootstrap.cs, RaceManager.cs | **"Sky Sentinel" demo spine**: MissionDirector runs intro → 3 escalating waves (clear to advance) → boss → Victory/Defeat with R restart + HUD objective readout; CombatSpawner shared enemy/boss factory (boss = 2.2× scale, faster/harder); player `freezeOnDeath` = mission Defeat (no silent respawn); RaceManager made inert in combat mode. Next: outposts + allied AI, escort/defend beat, mid-mission upgrade | session-050

- 2026-07-04 | DroneFlightController.cs, ParkMapGenerator.cs | **Collision push-off** (slide off walls instead of hard-stalling; floors/platforms ignored so you can still rest); **clustered-maze walls** (connected multi-segment wall runs that turn ~90° → corridors) | session-049

- 2026-07-04 | DroneWeapon.cs, SinglePlayerBootstrap.cs | Feel fixes: near-massless projectiles (0.05) so hits barely knock the drone; enemies spawn farther out (`enemySpawnDistance` 75m from centre) so you're not hit at spawn | session-048

- 2026-07-04 | TouchFlightHUD, SinglePlayerBootstrap, ParkMapGenerator, Pickup(new), TargetHealth, DroneWeapon | Fix radar MissingReferenceException on destroyed targets; force-remove magenta dummies; add **tall towers + hedge walls** to the park (maze/cover above ground); **pickups** — floating ammo caches (`DroneWeapon.ResupplyAmmo`) + health packs (`TargetHealth.Heal`) that respawn on a timer | session-047

- 2026-07-04 | ParkMapGenerator.cs (new), SinglePlayerBootstrap.cs | Greybox **children's-park map**: procedural primitive props (trees, slides, swings, jungle gyms, sandboxes, see-saws) + perimeter fence for cover/verticality/landmarks; ~110m medium arena. Art swaps onto the same layout later | session-046

- 2026-07-04 | DroneWeapon, TargetHealth, Projectile, DroneFlightController, EnemyDroneAI, SinglePlayerBootstrap, TouchFlightHUD, ArenaBounds(new) | Combat pass 2: faster projectiles (bullet 220 / rocket 140); **limited ammo** (bullet magazine + reload, regenerating rockets; enemies infinite) + HUD ammo; **hit feedback** (red damage flash + hitmarker via TargetHealth.OnAnyDamaged); enemies **don't respawn** on death + tinted red; **player HP bar** + HP% in HUD; **vertical-flight fix** (bank/upright assist fades near vertical, `bankStrength`); **hard arena boundary** (ArenaBounds sphere clamp) | session-045

- 2026-07-04 | HealthBar.cs (new), EnemyDroneAI.cs, TargetHealth.cs, TouchFlightHUD.cs, RaceTrackGenerator.cs, RaceManager.cs, SinglePlayerBootstrap.cs | Combat-sandbox tweaks: `buildGates`/`spawnTargets` default OFF (no rings/dummies, keep ground, fly immediately — no countdown); enemy fire rate slowed (`EnemyDroneAI.fireInterval` 0.5s); clearer `[HIT]` console log; floating billboard `HealthBar` on enemies; player HP% in the HUD readout | session-044

- 2026-07-04 | EnemyDroneAI.cs (new), TargetHealth.cs, TargetingSystem.cs, Projectile.cs, DroneWeapon.cs, DroneFlightController.cs, FlightInputRouter.cs, SinglePlayerBootstrap.cs | **Tier-1 enemy AI**: EnemyDroneAI is an `IFlightInput` source → reuses DroneFlightController + DroneWeapon (seek/engage/orbit/evade + fire). Added **factions** (`TargetHealth.team`; targeting + projectile damage cross-team only), camera-optional targeting/aim for AI, router auto-provision deferred to Start (enemies don't build a HUD), countdown-lock exemption for AI. Bootstrap spawns N enemies (team 2) + gives the player TargetHealth (team 1); dummies = team 2 | session-043

- 2026-07-01 | TouchFlightHUD.cs | Mini-radar (top-left): pooled blips for TargetHealth within `radarRange`, positioned relative to drone heading (up = forward), self dot + forward tick, locked target highlighted green | session-042

- 2026-07-01 | CameraController.cs, TouchFlightHUD.cs | FPV hides the drone's own mesh (was blocking the view); flight-vector marker changed from a dot to a green upward chevron (∧) | session-041

- 2026-07-01 | TouchFlightHUD.cs | Instruments pass: weapon reticle → corner-bracket square that scales with target distance; artificial horizon (fixed reference dashes + rolling/pitching horizon bar); green flight pip kept as the '-o-' center; optional right-stick flick-to-switch target (to compare vs the button) | session-040

- 2026-07-01 | TargetHealth.cs, TargetingSystem.cs, TouchFlightHUD.cs | Target selection priority = class (`TargetHealth.targetPriority`) > closest-to-center > distance (re-selects on pop/switch); added flight-vector pip (true heading) + compact SPD/ALT readout; shrank all on-screen buttons + labels for mobile real-estate. Next: mini-radar + artificial horizon; later: full PUBG-style HUD layout customization (all rects/sizes already inspector-exposed) | session-039

- 2026-07-01 | TargetingSystem.cs, TouchFlightHUD.cs, DroneWeapon.cs | Aim polish: shots now aim **fully** onto the lock (assistStrength 1, angle-gated) so bullets hit the crosshair at screen edges too; lock is kept when the target leaves the FOV and the crosshair becomes an **edge arrow** pointing to it; DroneWeapon now fires from a **muzzle array** (default single) to groundwork double-turret upgrades | session-038

- 2026-07-01 | CameraController.cs, TargetingSystem.cs, DroneWeapon.cs, TouchFlightHUD.cs | Combat aim overhaul: **free-look camera** (right stick orbits the view, decoupled from flight, recenters on release; fire down the view); **full-AI crosshair** snaps onto the nearest on-screen `TargetHealth` and weapons bend onto the lock; **target switching** via RS-click / Tab / on-screen TARGET button (radar candidate list, nearest-default). Removed the separate red lock marker (crosshair is the indicator) | session-037

- 2026-07-01 | FlightInputRouter.cs, FlightInputState.cs, GamepadFlightInput.cs, DroneFlightController.cs, TouchFlightHUD.cs | FreeAim is now the default aim mode; added juke/dodge dash (gamepad X / Left Ctrl → instant burst in the left-stick direction, cooldown-gated; touch swipe TBD) | session-036

- 2026-07-01 | TargetingSystem.cs (new), DroneWeapon.cs, TouchFlightHUD.cs | Aim-assist v1: soft lock-on picks the most-centered TargetHealth in a forward cone; weapon fire magnetizes toward it (`assistStrength`/`maxAssistAngle`); HUD shows a lock marker on the target. Reusable by enemy AI (same component, aimed at the player) | session-035

- 2026-07-01 | 🎮 Combat & Game Vision.md (new) | Brainstorm doc: Future Cop: LAPD adaptation list, aim-assist/lock-on targeting model, the "Sky Sentinel" vertical-slice demo, expansion/scale/funding view, and enemy-AI difficulty (AI = just another IFlightInput source) | session-034

- 2026-07-01 | DroneFlightController.cs, SinglePlayerBootstrap.cs, GamepadFlightInput.cs | Aim fixes: auto-level OFF by default (was springing the nose back to level and breaking hold-to-aim; kept as an easy-mode toggle, forced off in bootstrap); free-aim right-stick vertical inverted so up = aim up (`invertAimY`); targets moved onto the gate ring | session-033
- 2026-07-01 | TargetHealth.cs, DroneWeapon.cs, SinglePlayerBootstrap.cs | Weapon tuning: bright emissive magenta targets; health-driven shrink (100%→50% size then pop); bullet dmg 20 (5 hits/dummy), rockets 100 (5x bullets) | session-032
- 2026-07-01 | FlightInputState/Router, GamepadFlightInput, TouchFlightHUD, DroneWeapon, Projectile (new), TargetHealth, SinglePlayerBootstrap | Weapons pass v1: nose-aim + right-stick free-aim (recenters) toggled by an AIM-MODE switch button; two fire buttons (bullets primary + dumb-splash rockets secondary); free-aim cone drives fire direction + moves the crosshair; pooled procedural projectiles; offline-friendly TargetHealth + procedural shooting-range dummies | session-031
- 2026-07-01 | TouchFlightHUD.cs, FlightInputRouter.cs | M1 controls: removed touch brake button + added sticky-neutral throttle (`throttleCenterDeadzone`); controller→HUD mirroring (fixed-home sticks reflect live merged input via `FlightInputRouter.Last`) with Mirror/Hide `controllerHudMode` (auto-detects connected gamepad) | session-030
- 2026-07-01 | DroneFlightController.cs, TouchFlightHUD.cs | Playtest fixes: `invertPitch` toggle (default flips vertical aim so push-up climbs); added `-o-` flight crosshair at screen center (`showCrosshair`) | session-029
- 2026-07-01 | GamepadFlightInput.cs, TouchFlightHUD.cs, DroneFlightController.cs, SinglePlayerBootstrap.cs, RaceManager.cs, controller-map.md, glossary.md | Playtest tuning (PC+controller): LEFT stick now aims (yaw/pitch), RIGHT translates (strafe/altitude) on gamepad + touch; throttle slider is now center-detent fwd/zero/reverse (`thrust` −1..1); top speed ~3x via `thrustMultiplier`, reverse at 0.6x, steering ×1.5; guarded kinematic-body velocity sets (silences warnings). Missed-gate recovery = loop around (already enforced by in-order gates) | session-028
- 2026-07-01 | Scripts/Racing/* , DroneFlightController.cs, TouchFlightHUD.cs | Race polish: dedicated gold start/finish line (distinct from gate 0) with cross-to-start + cross-to-finish lap logic; 3-2-1-GO countdown that freezes the drone (RaceManager.FlightLocked + kinematic); restart via on-screen RESTART button and R key | session-027
- 2026-07-01 | Scripts/Input/* (new), DroneFlightController.cs, CameraController.cs, Scripts/Racing/* (new), controller-map.md, tasks/active.md | M1 touch flight toy: device-agnostic input layer (IFlightInput + router), on-screen touch HUD with TwinStick/MoveAndDrag toggle + throttle slider + brake + view button, offline single-player play (IsSpawned-gated control), auto-level assist, and a procedural circular hoop-racing circuit (RaceGate/RaceManager/RaceTrackGenerator + SinglePlayerBootstrap) | session-026
- 2026-06-30 | code-review.md, DEC-003, Session Logs/_index, frontmatter | Reviewed 12 C# scripts (found critical non-networked-projectile combat bug); deep-dived netcode and wrote DEC-003 (lean switch NGO→Photon Fusion); added session-log MOC + YAML frontmatter to key docs | session-025
- 2026-06-30 | Brain-wide audit | Created 6 missing referenced files (invariants, gotchas, conventions, assumptions, verification, debugging); fixed dead links; rewrote stale active.md; standardized DEC-001 + thin rubrics + API sandbox; added roles/file-index; stripped BOMs | session-024
- 2026-06-30 | UDAAN_ROUTER.md | Registered 3 strategy docs; fixed dangling wikilinks to real files; added basename-link note, dual-naming explainer, file index, and planned-files list | session-023
- 2026-06-30 | Master Plan.md, UDAAN_MASTER_PROJECT_PLAN.md, UDAAN_Analysis_and_Competition.md | Added milestone roadmap (M1–M6, risk-sequenced); split full plan into Execution doc + Analysis/Competition doc inside Udaan-Brain so wiki links resolve | session-022
- 2026-06-26 | DroneFlightController.cs | Stripped legacy keyboard inputs to strictly enforce DRONE-10 definitive gamepad axis blueprint mappings | session-021
- 2026-06-26 | architecture/controller-map.md, UDAAN_ROUTER.md | Migrated gamepad schema and aligned Core Input structural map inside UDAAN_ROUTER.md | session-020
- 2026-06-26 | DroneFlightController.cs | Rewrote flight controller to execute the Ace Combat FPS Hybrid flight engine and mapped unified fallback schemas | session-019
- 2026-06-26 | ClassSelectionUI.cs | Added and bound explicit Host/Client UI buttons to flush cached class selection | session-018
- 2026-06-26 | ClassSelectionUI.cs, DroneWeapon.cs, DroneFlightController.cs | Restored Xbox input profiles and refactored UI routing for explicit Host/Client connections | session-017
- 2026-06-26 | ClassSelectionUI.cs | HOTFIX: Refactored selection buttons to implicitly start the network host and wait for initialization before sending RPC | session-016
- 2026-06-26 | DroneWeapon.cs, NetworkUI.cs | Suppressed mouse weapon firing and removed legacy OnGUI network buttons to clean up flight calibration sandbox | session-015
- 2026-06-26 | DroneFlightController.cs, TargetHealth.cs | Implemented sandbox respawn loop for Target Dummy and enhanced network validation logging | session-014
- 2026-06-26 | ClassSelectionUI.cs | Created ClassSelectionUI to bind player interface buttons to the DroneClassSpawner RPCs | session-013
- 2026-06-26 | DroneFlightController.cs, TargetHealth.cs, DroneClassSpawner.cs | HOTFIX: Updated InitializeClassData signature to accept DroneClassData, resolving CS1061. Documented defaultClassData in glossary. | session-012
- 2026-06-26 | DroneClassSpawner.cs | Created Multi-Class Selection Network Spawner using ServerRpc and registered availableClasses in glossary | session-011
- 2026-06-26 | UDAAN_ROUTER.md | Enforced Glossary Maintenance Rules to strictly govern future variable modifications | session-010
- 2026-06-26 | architecture/glossary.md | Created Architecture Data Glossary to track active variables in core gameplay loops | session-009
- 2026-06-26 | DroneFlightController.cs, TargetHealth.cs | Hooked up DroneClassData to dynamically overwrite flight physics and max health at runtime | session-008
- 2026-06-26 | DroneClassData.cs | Created DroneClassData ScriptableObject blueprint template with health and physics parameters | session-007
- 2026-06-19 | PlayerHUDController.cs | Implemented dynamic runtime linkage via HUD_ScrapText tags, separating scene-space display meshes from network prefab assets | session-006
- 2026-06-19 10:45 | ECON-05: Created PlayerHUDController to display player's scrap count dynamically with defensive checks.
- 2026-06-19 00:50 | ECON-04: Implemented DroneUpgradeData ScriptableObject and TryPurchaseUpgrade logic in PlayerEconomy with an OnSpeedModifierUpgraded event hook.
- 2026-06-19 00:48 | ECON-03: Created PlayerEconomy script to manage player scrap count via trigger collisions, and added hovering/spinning animations to ScrapItem.
- 2026-06-16 01:30 | ECON-01 & ECON-02: Created ScrapItem script and updated TargetHealth to spawn network-synced scrap with explosive force on death.
