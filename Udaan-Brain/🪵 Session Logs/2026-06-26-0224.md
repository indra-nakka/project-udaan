---
id: 2026-06-26-0224
type: session
status: active
task: HOTFIX
related: []
---

## 🎯 Goal
Execute Network UI orchestration hotfix to ensure `ClassSelectionUI.cs` robustly initializes the Host connection prior to firing selection RPCs.

## 🕵️ Assumptions Going In
- I assume a coroutine with `WaitUntil` checking for `IsConnectedClient` and `PlayerObject` instantiation is the safest way to ensure NGO is fully ready to process ServerRpc commands.

## ⚙️ Execution Mode
DFS — Surgical hotfix following precise instructions to inject a frame step/IsListening check.

## 📝 Actions Log
- 02:24 — Read `UDAAN_ROUTER.md` to initialize the session properly.
- 02:24 — Refactored click listeners in `ClassSelectionUI.cs` into a `HandleClassSelection` Coroutine.
- 02:24 — Script now implicitly calls `NetworkManager.Singleton.StartHost()` if the network isn't listening, then yields execution until the `PlayerObject` safely spawns before transmitting the RPC.

## ✅ Verification
- [ ] Code compiles without Editor errors.
- [x] All rules in `architecture/invariants.md` respected.

## 📌 Summary (For `sessions/_index.md` & `changelog.md`)
HOTFIX: Refactored selection buttons to implicitly start the network host and wait for initialization before sending RPC.
