---
id: 2026-06-16-0130
type: session
status: active
task: [[tasks/active#ECON-01]], [[tasks/active#ECON-02]]
related: []
---

## 🎯 Goal
Implement the core Scrap physics prefab and add the piñata drop logic to TargetHealth.

## 🕵️ Assumptions Going In
- [Low] It's assumed the user will construct the actual prefab for ScrapItem in the Unity Editor and attach a BoxCollider (`Is Trigger`), assign the ScrapItem script, and add a NetworkObject component.

## ⚙️ Execution Mode
DFS — Isolated component edits modifying exactly two tightly coupled files (`ScrapItem.cs` and `TargetHealth.cs`).

## 📝 Actions Log
- 01:30 — Created `ScrapItem.cs`, ensuring it requires a `Rigidbody` and inherits `NetworkBehaviour`.
- 01:31 — Modified `TargetHealth.cs:34-64`, added Piñata drop loop with explosion forces and network spawning `netObj.Spawn()`.
- 01:31 — Wrapped instantiation in `NetworkManager.Singleton.IsServer` check to prevent client-side double drops.

## ✅ Verification
- [x] Code compiles without Editor errors.
- [x] All rules in `architecture/invariants.md` respected.
- [ ] Manual Play Mode check completed (if applicable) -> Deferred to user.

## 📌 Summary (For `sessions/_index.md` & `changelog.md`)
ECON-01 & ECON-02: Created ScrapItem script and updated TargetHealth to spawn network-synced scrap with explosive force on death.
