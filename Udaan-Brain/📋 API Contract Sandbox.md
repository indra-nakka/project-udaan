# 📋 API Contract Sandbox

Keep raw JSON payload schemas here. When we write the Node.js server to assign UDP match rooms, you can copy-paste the contract directly to ensure the client-side C# requests match your Express routes perfectly.
